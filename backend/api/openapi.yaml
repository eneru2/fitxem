openapi: 3.0.3
info:
  title: Just Clock API
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check
  /auth/register-org:
    post:
      summary: Register organization
  /auth/login:
    post:
      summary: Login
  /clock/in:
    post:
      summary: Clock in
